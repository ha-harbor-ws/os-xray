# BGP peers — one include per protocol name under /usr/local/etc/bird/
include "/usr/local/etc/bird/peer_refilter.inc";
include "/usr/local/etc/bird/peer_antifilter_download.inc";
include "/usr/local/etc/bird/peer_antifilter_network.inc";
